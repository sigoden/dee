{
  "name": "@sigodenjs/dee-test-utils",
  "version": "0.8.0",
  "description": "Test utility functions for dee framework",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "lint": "tslint -p tsconfig.json --fix",
    "build": "tsc",
    "build:prod": "tsc -p tsconfig.prod.json",
    "clean": "rimraf dist",
    "prepublish": "npm run -s clean && npm run -s build:prod"
  },
  "files": [
    "dist",
    "swagger.yaml"
  ],
  "keywords": [],
  "author": "Sigoden Huang <sigoden@gmail.com> (https://sigoden.com/)",
  "license": "MIT",
  "dependencies": {
    "@sigodenjs/dee": "^0.8.0",
    "@types/node": "^10.9.4"
  },
  "devDependencies": {
    "rimraf": "^2.6.2",
    "typescript": "^3.0.3"
  }
}
